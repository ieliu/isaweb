export default function MediaRatings() {
  return (
    <main
      style={{
        minHeight: "100vh",
        background: "var(--color-soft-yellow)",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <h1 style={{ fontFamily: "'HamIsCute'", fontSize: "3.5rem", color: "var(--color-text-primary)" }}>
        media ratings
      </h1>
    </main>
  );
}
